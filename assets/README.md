# Assets

Place your application icons here:

- `icon.png` - Application icon (PNG format, 256x256 or higher)
- `icon.ico` - Windows icon file (ICO format, multiple sizes)

For now, the app will use a placeholder icon. Replace these files with your actual application icons before packaging for distribution.
